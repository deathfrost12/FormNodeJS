doctype html 
html 
    head 
        title List 
        link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css')
    body(style="background-color:black;")
        header.jumbotron-fluid.bg-dark.p-2 
            h1.text-center.text-white.display-4 #{title}
        main
            table(class="table table-striped table-bordered table-dark")
                thead 
                    tr.bg-dark.text-light 
                        th  Jak dlouho průměrně trvá, než usnete?
                        th  Kolik hodin spíte průměrně za noc?
                        th  Jaké je vaše obvyklé časové rozmezí spánku?
                        th  Jak často se probouzíte během noci?
                        th  Jak často se cítíte unaveni během dne?
                tbody 
                    each data in records
                        tr 
                            td #{data.casDoUsnuti}
                            td #{data.prumerSpanku}
                            td #{data.rozmeziSpanku}
                            td #{data.probouzeni}
                            td #{data.unavenost}
            footer
                p.float-end.me-3 
                    a(href="/" type="button" class="btn btn-dark btn-outline-light") New
            script(src='https://code.jquery.com/jquery-3.4.1.slim.min.js') 
            script(src='https://getbootstrap.com/docs/4.4/dist/js/bootstrap.bundle.min.js')